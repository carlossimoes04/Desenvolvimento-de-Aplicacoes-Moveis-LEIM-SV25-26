# Features

1. Fetch images from the Picsum Photos API.
2. Display images in a scrollable list (RecyclerView) using XML layouts.
3. Refresh images via a user action (refresh button or swipe-to-refresh).
4. Show a loading indicator while data is being fetched from the API.